<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="recycling.buyer.dao.face.BuyerDao">

	<insert id="insertOrder" parameterType="Orders">
		INSERT INTO orders (ORD_CODE,B_CODE,ORD_NAME,ORD_PHONE,SEND_NAME,SEND_PHONE,ORD_POSTCODE,ORD_ADDR,ORD_DETAIL,ORD_MEMO,ORD_DATE,ORD_PAY,ORD_SUM, ORD_FEE)
		VALUES(#{ordCode},#{bCode},
		(SELECT b_name FROM buyer WHERE b_code = #{bCode}),
		(SELECT b_phone FROM buyer WHERE b_code = #{bCode})
		, #{sendName}
		, #{sendPhone}
		, #{ordPostcode}
		, #{ordAddr}
		, #{ordDetail}
		, #{ordMemo}
		, sysdate
		, #{ordPay}
		, #{ordSum}
		, #{ordFee})
	</insert>
	
	<select id="selectAllCart" parameterType="String" resultType="CartOrder">
		SELECT
			C_CODE, S_CODE, c.PRD_CODE, C_CNT, PRD_NAME, PRICE, PRD_FEE, stored_name
		FROM
			cart c, prd p, prd_file pf
		<where>
			AND c.PRD_CODE =p.PRD_CODE
			AND p.PRD_CODE = pf.PRD_CODE
			AND B_CODE = #{bCode}
		</where>
		ORDER BY C_CODE
	</select>
	
	<select id="selectBycCode" parameterType="String" resultType="CartOrder">
		SELECT
			C_CODE, S_CODE, c.PRD_CODE, C_CNT, PRD_NAME, PRICE, PRD_FEE, stored_name
		FROM
			cart c, prd p, prd_file pf
		<where>
			AND c.PRD_CODE = p.PRD_CODE
			AND p.PRD_CODE = pf.PRD_CODE
			AND C_CODE = #{cCode}
		</where>
		ORDER BY C_CODE
	</select>
	
	<select id="selectBybCode" parameterType="String" resultType="BuyerAdr">
		SELECT
		    adr_name, adr_phone, adr_postcode, adr_addr, adr_detail
		FROM buyer_adr
		<where>
			AND b_code = #{bCode}
			AND adr_chk = 'Y'
		</where>
		ORDER BY ADR_CODE
	</select>
	
	<select id="selectByordCode" parameterType="String" resultType="Orders">
		SELECT * FROM orders
		WHERE ORD_CODE = #{ordCode}
		ORDER BY ORD_CODE
	</select>
	
	<select id="selectPrdCnt" parameterType="String" resultType="Integer">
		SELECT prd_cnt FROM prd
		<where>
			PRD_CODE = #{prdCode}
		</where>
	</select>
	
	<delete id="deleteCart" parameterType="String">
		DELETE FROM cart
		WHERE c_code = #{cCode}
	</delete>
	
	<update id="updatePrdCnt" parameterType="CartOrder">
		UPDATE prd
	    SET prd_cnt = prd_cnt-#{cCnt}
	    WHERE prd_code = #{prdCode}
	</update>
	
	<insert id="insertOrderDetail" parameterType="OrderDetail">
		INSERT INTO order_detail VALUES (
		    'ORDT' || lpad(cart_seq.nextval, '6', 0)
		    , #{ordCode}
		    , #{prdCode}
		    , #{ordName}
		    , #{ordPrice}
		    , #{ordCnt}
		    , #{ordSum}
		    , #{sttNo}
		)
	</insert>
	
	<select id="buyerDaoselectOrderDetailBybCode" parameterType="String" resultType="MyOrder">
		SELECT 
			ORDDT_CODE, od.ORD_CODE, PRD_CODE, od.ORD_NAME, ORD_PRICE, ORD_CNT, od.ORD_SUM, STT_NO, ORD_DATE
		FROM 
			order_detail od, orders o
		<where>
			AND o.ord_code = od.ord_code
			AND o.b_code = #{bCode}
		</where>
	</select>
	
	<update id="updatecCnt" parameterType="Cart">
		UPDATE cart
		SET c_cnt = #{cCnt}
		<where>
			AND c_code = #{cCode}
		</where>
	</update>

	<!-- 구매자 아이디로 구매자 정보 조회 -->
	<select id="selectBuyerBybId" parameterType="string" resultType="recycling.dto.buyer.Buyer">
		SELECT B_Code AS bCode,
		       B_Ct_Code AS bCtCode,
		       B_Id AS bId,
		       B_Pw AS bPw,
		       B_Name AS bName,
		       B_Phone AS bPhone,
		       B_Email AS bEmail,
		       Ad_Sms AS adSms,
		       Ad_Email AS adEmail
		FROM Buyer
		<where>
			AND B_Id = #{bId }
		</where>
	</select>
	
	<!-- 구매자 등급 조회 -->
    <select id="selectBuyerRank" parameterType="int" resultType="recycling.dto.buyer.BuyerRank">
    	SELECT Rank_No AS rankNo,
    		   Rank_Name AS rankName
    	FROM Buyer_Rank
    	<where>
    		AND Rank_No = #{rankNo }
    	</where>
    </select>
	
	<!-- 구매자 코드로 기업 정보 조회 -->
	<select id="selectCmpBybCode" parameterType="string" resultType="recycling.dto.buyer.Cmp">
        SELECT Cmp_No AS cmpNo,
        	   B_Code AS bCode,
               Cmp_Name AS cmpName,
               Cmp_Ceo AS cmpCeo,
               Cmp_Postcode AS cmpPostcode,
               Cmp_Addr AS cmpAddr,
               Cmp_Detail AS cmpDetail
        FROM Cmp
        <where>
        	AND B_Code = #{bCode }
        </where>
    </select>
    
    <!-- 구매자 정보 업데이트 -->
    <update id="updateBuyer" parameterType="recycling.dto.buyer.Buyer">
        UPDATE Buyer
        SET 
        	<if test="bPw != null">
        		B_Pw = #{bPw },
        	</if>
            B_Name = #{bName },
            B_Phone = #{bPhone },
            B_Email = #{bEmail },
            Ad_Sms = #{adSms },
            Ad_Email = #{adEmail }
        <where>
        	AND B_Code = #{bCode }
        </where>
    </update>
    
    <!-- 기업 정보 업데이트 -->
    <update id="updateCmp" parameterType="recycling.dto.buyer.Cmp">
        UPDATE Cmp
        SET Cmp_Name = #{cmpName },
            Cmp_Ceo = #{cmpCeo },
            Cmp_No = #{cmpNo },
            Cmp_Postcode = #{cmpPostcode },
            Cmp_Addr = #{cmpAddr },
            Cmp_Detail = #{cmpDetail }
        <where>
        	AND B_Code = #{bCode }
        </where>
    </update>
	
	<!-- 구매자의 모든 배송지 정보 가져옴 -->
    <select id="getBuyerAdr" parameterType="string" resultType="recycling.dto.buyer.BuyerAdr">
    	SELECT *
    	FROM Buyer_Adr
    	<where>
    		AND B_Code = #{bCode }
    	</where>
    	ORDER BY Adr_Chk DESC, Adr_Code ASC
    </select>
    
    <!-- 구매자 배송지 개수 -->
    <select id="cntBuyerAdr" parameterType="string" resultType="int">
    	SELECT count(*)
    	FROM Buyer_Adr
    	<where>
    		AND b_Code = #{bCode }
    	</where>
    </select>
    
    <!-- 새로운 배송지 등록 -->
    <insert id="insertBuyerAdr" parameterType="recycling.dto.buyer.BuyerAdr">
    	INSERT INTO Buyer_Adr (
    		Adr_Code, B_Code, Adr_Name, Adr_Phone, Adr_Postcode, Adr_Addr, Adr_Detail, Adr_Chk
    	) VALUES (
    		#{adrCode }, #{bCode }, #{adrName }, #{adrPhone }, #{adrPostcode }, #{adrAddr }, #{adrDetail }, #{adrChk }
    	)
    </insert>
    
    <!-- 배송지 정보 수정 -->
    <update id="updateBuyerAdr" parameterType="recycling.dto.buyer.BuyerAdr">
    	UPDATE Buyer_Adr
    	SET 
    		Adr_Name = #{adrName }, 
    		Adr_Phone = #{adrPhone }, 
    		Adr_Postcode = #{adrPostcode }, 
    		Adr_Addr = #{adrAddr }, 
    		Adr_Detail = #{adrDetail }
    	<where>
    		Adr_Code = #{adrCode }
    	</where>	
    </update>
    
    <!-- 배송지 삭제 -->
    <delete id="deleteBuyerAdr" parameterType="string">
    	DELETE FROM Buyer_Adr
    	<where>
    		AND Adr_Code = #{adrCode }
    	</where>
    </delete>
    
    <!-- 모든 기본 배송지를 초기화 -->
    <update id="resetDefaultAdr" parameterType="string">
    	UPDATE Buyer_Adr
    	SET Adr_Chk = 'N' 
    	<where>
    		AND B_Code = #{bCode}
    		AND Adr_Chk = 'Y'
    	</where>	
    </update>
    
    <!-- 선택한 배송지를 기본 배송지로 설정 -->
    <update id="setDefaultAdr" parameterType="string">
    	UPDATE Buyer_Adr
    	SET Adr_Chk = 'N'
    	<where>
    		AND Adr_Code = #{adrCode }
    	</where>
    </update>
    
    <!-- 구매자 탈퇴 -->
    <delete id="deleteBuyer" parameterType="string">
    	DELETE FROM Buyer
    	<where>
    		AND B_Code = #{bCode }
    	</where>
    </delete>
</mapper>